---
name: "Organize: Auto-Close Issues"

# +-------------------------------------------+
# |                                           |
# |    DO NOT EDIT DIRECTLY !!!!!             |
# |                                           |
# |    File is generated by jarvis.           |
# |    Update file in the jarvis module so    |
# |    you can apply changes to all repos.    |
# |                                           |
# +-------------------------------------------+

on:
  push:
    branches:
      - main
    paths:
      - .github/workflows/organize-auto-close-issues.yml
  schedule:
    - cron: '40 6 * * 1' # https://crontab.guru/#40_6_*_*_1

permissions:
  contents: read
  issues: write

jobs:
  auto-close-cancelled:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Close issues
        uses: bdougie/close-issues-based-on-label@master
        env:
          LABEL: cancelled
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  on-failure:
    runs-on: ubuntu-latest
    needs: ['auto-close-cancelled']
    if: failure()

    steps:
      - name: Send Pipeline Status to Google Chat
        if: always()
        uses: Co-qn/google-chat-notification@releases/v1
        with:
          name: ${{ github.workflow }}
          url: ${{ secrets.GOOGLE_CHAT_WEBHOOK }}
          status: failure
