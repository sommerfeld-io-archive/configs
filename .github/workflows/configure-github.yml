---
name: "Apply global Github config"

on:
  push:
    branches:
      - main
    paths:
      - .github/workflows/configure-github.yml
      - src/main/github
  schedule:
    - cron: '0 6 * * 1' # https://crontab.guru
  workflow_dispatch:

permissions:
  contents: read
  issues: write

jobs:
  lint-terraform:
    name: Lint Terraform config
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      - name: Debug
        run: pwd && ls -alF
      - name: tflint
        run: |
          (
            cd src/main/github || exit
            ./apply-config.sh lint ${{ secrets.GITHUB_TOKEN }}
          )
        shell: bash

  # TODO run terratest etc ...

  apply-github-config:
    name: Apply config by running Terraform
    runs-on: ubuntu-latest
    needs: lint-terraform
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      - name: Debug
        run: pwd && ls -alF
        shell: bash
      - name: initialize
        run: |
          (
            cd src/main/github || exit
            ./apply-config.sh init ${{ secrets.GITHUB_TOKEN }}
          )
        shell: bash
      - name: validate
        run: |
          (
            cd src/main/github || exit
            ./apply-config.sh validate ${{ secrets.GITHUB_TOKEN }}
          )
        shell: bash
      - name: plan
        run: |
          (
            cd src/main/github || exit
            ./apply-config.sh plan ${{ secrets.GITHUB_TOKEN }}
          )
        shell: bash
      - name: apply
        run: |
          (
            cd src/main/github || exit
            ./apply-config.sh apply ${{ secrets.GITHUB_TOKEN }}
          )
        shell: bash

  generate-docs:
    name: Generate Adoc contents
    runs-on: ubuntu-latest
    needs: apply-github-config
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      - name: format
        run: |
          (
            cd src/main/github || exit
            ./apply-config.sh fmt ${{ secrets.GITHUB_TOKEN }}
          )
        shell: bash
      - name: Copy help.adoc to Antora partial
        run: echo "Implement me ..."
      - name: Generate some other docs
        run: echo "Implement me ..."
      - name: Commit and Push
        run: echo "Implement me ..."

  on-failure:
    runs-on: ubuntu-latest
    needs: ['lint-terraform', 'apply-github-config', 'generate-docs']
    if: failure()
    steps:
      - name: Send Pipeline Status to Google Chat
        if: always()
        uses: Co-qn/google-chat-notification@releases/v1
        with:
          name: ${{ github.workflow }}
          url: ${{ secrets.GOOGLE_CHAT_WEBHOOK }}
          status: failure
